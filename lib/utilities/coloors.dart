import 'package:flutter/material.dart';

class Coloors {
  Coloors._();

  // Primary Colors
  static const Color greenDark = Color(0xFF00A884);  // WhatsApp's primary accent color in dark mode
  static const Color greenLight = Color(0xFF008069); // WhatsApp's primary accent color in light mode
  
  // Secondary Colors
  static const Color blueDark = Color(0xFF53BDEB);   // Message selection color in dark mode
  static const Color blueLight = Color(0xFF027EB5);  // Message selection color in light mode
  
  // Text & Icon Colors
  static const Color greyDark = Color(0xFF8596A0);   // Secondary text in dark mode
  static const Color greyLight = Color(0xFF667781);  // Secondary text in light mode
  
  // Background Colors
  static const Color backgroundDark = Color(0xFF121B22);  // Main background in dark mode
  static const Color backgroundLight = Color(0xFFFFFFFF); // Main background in light mode
  static const Color greyBackground = Color(0xFF1F2C34);  // App bar and modal background in dark mode
  
  // Additional WhatsApp Dark Mode Colors
  static const Color chatBubbleDark = Color(0xFF1F2C34);  // Received message bubble in dark mode
  static const Color outgoingChatBubbleDark = Color(0xFF005C4B);  // Sent message bubble in dark mode
  static const Color dividerDark = Color(0xFF262D31);     // Divider color in dark mode
  static const Color statusBarDark = Color(0xFF1F2C34);   // Status bar color in dark mode
  static const Color iconDark = Color(0xFF8596A0);        // Icon color in dark mode
  static const Color textDark = Color(0xFFE9EDEF);        // Primary text in dark mode
  static const Color secondaryTextDark = Color(0xFF8596A0); // Secondary text in dark mode
}